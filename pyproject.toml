[build-system]
requires = ["setuptools>=61.2",
            "setuptools_scm[toml]>=3.4.3",
            "wheel"]
build-backend = 'setuptools.build_meta'

[project]
name = "novt"
description = "Package description"
authors = [{name = "STScI", email = "help@stsci.edu"}]
license = {file = "LICENSE.rst"}
dynamic = ["version"]
requires-python = ">=3.9"
dependencies = [
    "astropy>=5.2",
    "jwst_gtvt>=0.3.1",
    "numpy>=1.24",
    "pandas>=1.4.0",
    "pysiaf>=0.19.1",
    "regions>=0.7"
]
classifiers = [
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Topic :: Scientific/Engineering :: Physics"
]

[project.optional-dependencies]
interact = [
    "bqplot>=0.12.37",
    "ipykernel>=6.19.4",
    "ipyvuetify>=1.7.0",
    "ipywidgets>=8.0.4",
    "jdaviz>=3.4.0",
    "traitlets>=5.0.5",
    "voila>=0.4"
]
test = [
    "flake8",
    "pytest",
    "pytest-coverage",
    "pytest-mock"
]
docs = [
    "graphviz",
    "stsci_rtd_theme",
    "sphinx-astropy",
    "sphinx_rtd_theme",
    "sphinx-automodapi",
    "sphinx-issues",
    "tomli"
]

[project.readme]
text = """
Project readme.
"""
content-type = "text/markdown"

[project.scripts]
novt = "novt.run_notebook:_main"

[project.urls]
Homepage = "https://github.com/spacetelescope/novt"

[tool.setuptools.package-data]
"*" = ["*.fits, *.csv"]
"novt" = ["data/*"]
"novt.tests" = ["data/*"]

[tool.setuptools_scm]
write_to = "novt/version.py"

[tool.pytest.ini_options]
minversion = "2.2"
testpaths = [
    "novt/tests",
    "novt/interact/tests"
]
norecursedirs = [
    "build",
    "docs/_build",
    "novt/ephemeris"
]
astropy_header = "true"
doctest_plus = "enabled"
text_file_format = "rst"
addopts = "-p no:warnings"


[tool.coverage.run]
source = ["novt",]
omit = [
    "novt/tests/*",
    "novt/version*",
    "novt/ephemeris/*"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "except ImportError",
    "raise AssertionError",
    "raise NotImplementedError",
    "def main(.*):",
    "pragma: py{ignore_python_version}",
]
omit = [
    "novt/tests/*",
    "novt/interact/tests/*",
    "novt/version*",
    "novt/ephemeris/*"
]
